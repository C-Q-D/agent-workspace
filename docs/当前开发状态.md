# AgentWorkspace 当前开发状态

## 快照信息

- 快照日期：2026-07-26
- 当前阶段：自用效率优先路线已完成 E001 入口切换，准备复验基线
- 执行模式：连续原子开发；每个原子验证通过后独立提交并推送当前 upstream
- 自主执行范围：E001～E139
- 首个人工门：E140
- 当前已完成范围：A001～A026 作为代码基线；E001 已完成
- 下一原子：E002 复验 A026 代码与测试基线
- 当前分支：`experiment/e01-pty-decoupling`
- 当前 upstream：`origin/experiment/e01-pty-decoupling`
- 自研远端：`git@github.com:C-Q-D/agent-workspace.git`

A003 的提交 SHA 是本文件首次进入 Git 历史的提交，可使用以下命令取得，避免在同一提交内
维护无法预先确定的自引用 SHA：

```powershell
git log -1 --format="%H %s" -- docs/当前开发状态.md
```

## 唯一执行计划

完整计划位于：

```text
docs\规划\自用效率优先完整代码原子任务方案.md
```

外层领航副本位于：

```text
F:\workspace\projects\AgentWorkspaceLab\agent-workspace\docs\ai-project\自用效率优先完整代码原子任务方案.md
```

恢复开发时必须先读取仓库内计划副本、外层项目工作台、外层项目阶段记录、仓库
`AGENTS.md` 和当前原子卡。不得用本快照替代完整计划，也不得仅凭对话记忆继续。

## 最近三个原子

| 原子 | 状态 | 提交或定位方式 | 验证 |
|---|---|---|---|
| A024 ActiveContext 单源派生 | 已完成并推送 | `8dcc4ed`、修复 `c32f520` | 状态 12、契约 11、应用 1482、配置 113、布局 5、Review 与真实文件引用通过 |
| A025 会话恢复单一事实源 | 已完成并推送 | `ecee8b2` | 会话 17、应用 1485、配置 115、布局 5、20 窗口 v2 重启、v1 迁移与零残留通过 |
| A026 删除重复上下文资源 | 本快照随提交完成并推送 | `git log -1 -- docs/当前开发状态.md` | 应用 1486、配置 115、布局 5；16 窗格 Grid 零上下文、Focused 单上下文、Review 单专用资源、20 窗口重启与零残留通过 |

## 当前代码与测试基线

- 代码基线来源：A001 的 `docs/当前仓库事实审查.md`。
- Release 用户数据根：`%USERPROFILE%\.agent-workspace`。
- Debug 用户数据根：`%USERPROFILE%\.agent-workspace-dev`。
- 主设置：`<数据根>\config\settings.json`。
- 工作区会话：`<数据根>\sessions\workspaces.json`。
- `paneflow-config`：115 passed。
- `paneflow-app` unit：1486 passed，0 ignored。
- `flex_nchild`：5 passed。
- 真实 Windows Release：16 窗格 Grid 为零活动上下文，Focused 为一个文件/Git
  上下文，Review 只保留一个专用审查资源；20 个窗口重启保持稳定 ID、活动项和矩阵页码，
  PowerShell PID 全部替换。Review A→B→A、会话恢复与退出零残留通过。
- 基线测试日期：2026-07-25（A026）。

以上测试数是 A026 的可重复基线，不是永久常量。后续检查点必须写入当次实际命令输出。

## 既有开发与新计划的衔接

在 A001～A003 之前，旧 P2 计划已完成：

- P2-01～P2-04。
- P2-05 UNIT-37～UNIT-39。
- UNIT-40 ATOM-40-01～02，旧停点提交为 `0c168e1`。

新计划不抹掉这些历史证据，但把尚未完成的设置真实验收重新收敛为：

- A004：固化十项设置组合验收脚本。
- A005：执行真实设置组合与生命周期验收。
- A006：封存设置收口证据并关闭 UNIT-40。
- A007～A009：稳定设置性能与总验收。

其中 A004～A006 已完成并关闭 UNIT-40 ATOM-40-03；A007～A009 已完成并关闭
UNIT-40 ATOM-40-04 与 P2-05。A010～A014 已完成公开产品面、默认构建依赖、展示状态、
资源生命周期和模块处置决策，V1-03 已关闭；A015～A020 已完成缺口测试、单一模型、
集中命令、核心聚焦路径、Review/Settings/恢复路径和旧双写删除，V1-04 已关闭；
A021 已固定创建、聚焦、切换、关闭、恢复、非 Git 初始化的会话所有权契约；
A022 已让 `WindowSession` 唯一持有稳定 ID/root、终端布局和生命周期查询；
A023 已把创建、关闭、退出补建、撤销关闭和模板替换迁入会话生命周期；
A024 已删除展示状态中的重复 root，文件树与 Git Review 从聚焦 WindowSession 派生；
A025 已把会话升级为 metadata-only v2 并保持稳定窗口 ID；
A026 已删除每工作区 Git watcher 与全仓库回退刷新，Grid、Focused、Review 的上下文资源
由展示状态确定性切换，V1-05 已关闭。公开发布路线完整保留并暂停在 A027；当前已切换到
自用效率优先路线，E001 已完成，下一项为 E002。

## 当前事实源职责

| 入口 | 职责 |
|---|---|
| `AGENTS.md` | 当前代码仓库有效开发规则 |
| `docs/规划/自用效率优先完整代码原子任务方案.md` | E001～E144 的仓库内权威计划副本 |
| `docs/当前仓库事实审查.md` | A001 时点的证据基线和冲突清单 |
| `docs/当前开发状态.md` | 每三个原子的代码仓库检查点 |
| 外层 `docs/ai-project/项目工作台.md` | 当前停点、跨上下文恢复和用户可见进度 |
| 外层 `docs/ai-project/项目阶段记录.md` | 标准路线阶段关口、产物和历史 |
| 外层 `docs/ai-project/自用效率优先完整代码原子任务方案.md` | 领航层计划副本与跨会话恢复入口 |
| 外层 `AGENTS.md` | 产品范围、优先级和功能台账 |

公开 `README.md`、`ARCHITECTURE.md` 和部分 `.github/` 文件仍有 Paneflow 遗留；在
恢复公开发布路线前，它们不是当前开发状态的事实源。

## 检查点同步规则

每完成三个原子以及每个阶段边界：

1. 重新读取完整原子计划和最新项目工作台。
2. 更新本文件的已完成范围、最近三个原子、实际测试和下一原子。
3. 更新外层项目工作台与阶段记录。
4. 记录每个原子的提交 SHA、测试命令、远端结果、修复原子和已知限制。
5. 推送代码仓库检查点，并向用户发送简洁进度。

若第三个原子是纯外部验收，仍须在最近一个可追踪代码仓库提交中保存检查点证据；不得只在
对话里说明完成。

## 当前阻塞与人工门

- E002 开始前没有已知工程阻塞；A026 静态、运行时、Review、会话和性能回归均通过，
  当前无 ignored。
- E001～E139 是自用效率优先自动区间；E140 及以后需要真实登录 Codex/Claude、图片体验
  与用户整体可用性验收，未取得对应明确决定前不得进入。
- 原 Windows 开源 v1 发布计划暂停在 A027；公开发布、CI、安装器和治理任务不属于当前入口。
- `paneflow/` 及其他样本仓库始终只读。
