# P2-01 独立产品身份验收报告

## 结论

P2-01 独立产品身份于 2026-07-19 验收通过。AgentWorkspace 的 Windows 用户数据、主程序、GUI、CLI、AUMID、MCP/Hook 服务键、公开链接、更新源和应用图标均已使用独立产品身份；内部保留的 `paneflow-*` crate、兼容迁移键和上游归属不属于公开身份回退。

## 交付范围

| 交付单元 | 结果 | 主要证据 |
| --- | --- | --- |
| UNIT-25 独立用户数据根目录 | 通过 | Release 使用 `%USERPROFILE%\.agent-workspace`，Debug 使用 `%USERPROFILE%\.agent-workspace-dev`；真实隔离写入和旧目录不读取验收通过 |
| UNIT-26 独立公开应用身份 | 通过 | `agent-workspace.exe`、AgentWorkspace GUI/CLI、`CQD.AgentWorkspace`、MCP/Hook 新服务键和独立 GitHub 链接通过真实 Release 验收 |
| UNIT-27 独立 Windows 图标 | 通过 | 独立主图、9 个 ICO 尺寸、PE 关联图标、真实 HWND 图标、WiX 图标和真实启动窗口通过验收 |

## 真实 Release 结果

- 可执行文件：`target/icon-atom/release/agent-workspace.exe`
- 版本输出：`agent-workspace 0.7.11`
- Release SHA-256：`8b22f61fd6096e56162ad1d73ac8deb0885d414036e50fb28521777058d6e351`
- `--help`：包含 AgentWorkspace、`Usage: agent-workspace` 和独立 GitHub 仓库入口。
- Windows AUMID：`CQD.AgentWorkspace`。
- 主 ICO 与 WiX ICO SHA-256：`6b41cb0bf32decc2c4eb5cbad2059529aa25f846a3c976fe4d119832a158d433`。
- 启动 Splash：真实 Release 截图显示 AgentWorkspace。

详细证据：

- [Windows 公开身份验收报告](Windows公开身份验收报告.md)
- [Windows 图标验收报告](Windows图标验收报告.md)
- [Windows 图标机器可读结果](Windows图标数据/Release-20260719-175034/运行结果.json)

## 总回归

| 测试 | 结果 |
| --- | --- |
| `cargo test -p paneflow-app` | 1425 个主程序测试与 5 个布局集成测试全部通过 |
| `cargo test -p paneflow-mcp-install` | 97 个 MCP/Hook 安装、迁移和公开身份测试全部通过 |
| `python scripts/生成Windows图标资产.py` | 确定性生成通过，ICO 尺寸和 WiX 镜像一致性通过 |
| 最新 Release CLI 冒烟 | `--help` 与 `--version` 独立身份通过 |

## 原子提交

- `fa1deb4`、`2c86007`、`e0b5aa3`：独立用户数据根目录。
- `4b2bb1c`、`453568d`、`0f1d8ef`、`25e60dc`、`0a755a7`：Windows GUI、CLI、MCP/Hook、链接和 Release 公开身份。
- `33d5de9`：独立 Windows 图标源资产与确定性生成。
- `4680c0f`：运行时、EXE 与 WiX 图标接入。
- `7e4bbb7`：真实 Release 图标验收与启动 Splash 身份修复。

所有提交均已推送到 `origin/experiment/e01-pty-decoupling`。

## 非目标与保留项

- 不在 P2-01 中全量重命名内部 `paneflow-*` crate、历史类型或兼容标记。
- 不静默迁移或读取 Paneflow 旧用户数据。
- 不扩展 macOS、Linux、WSL 或 SSH 图标与发布流程。
- 当前图标是可替换的第一版产品资产，后续品牌精修不阻塞 v1.0 工程工作。
