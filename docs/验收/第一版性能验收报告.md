# 第一版性能验收报告

## 当前结论

截至 2026-07-18，1、9、16 个真实 Windows PowerShell/ConPTY 终端的容量与切换验收通过。16 终端主进程峰值 Working Set、三个容量点的主进程平均 CPU、窗口切换 P95、后台输出完整性、PTY 进程稳定性和关闭清理均满足第一版预算。

30 分钟长稳门禁尚未计入本结论；完成后应在本报告追加长稳结果，才能将 UNIT 13 整体标记为完成。

## 验收环境

- 操作系统：Windows 11 专业版，Build 26200。
- 处理器：13th Gen Intel(R) Core(TM) i7-13700H，20 个逻辑处理器。
- 内存：31.6 GiB。
- 构建：启用 `terminal-perf-metrics` 的 Release 二进制。
- 二进制 SHA-256：`14ACD3BB04008C89BF56AD79D29F4C2B72031EAC85B3E0639E0C08514E73CED1`。
- 负载：每个 workspace 启动独立的真实 PowerShell 子进程，每 250 ms 输出一条带窗口编号和时间戳的记录；未使用 mock 终端或伪造进程数据。
- 采样：每个容量点连续采样 30 秒，随后读取全部终端最终标记、执行 32 次真实 `surface.focus` 切换并正常关闭应用。

## 容量结果

| 终端数 | 主进程平均 CPU | 峰值 Working Set | 峰值 Private | 进程/PID 稳定 | 切换 P95 | 最终标记 | 关闭残留 |
| ---: | ---: | ---: | ---: | :---: | ---: | :---: | ---: |
| 1 | 0.1945% | 184.156 MiB | 153.777 MiB | 是 | 63.123 ms | 完整 | 0 |
| 9 | 0.4351% | 213.773 MiB | 214.801 MiB | 是 | 63.284 ms | 完整 | 0 |
| 16 | 0.5724% | 231.289 MiB | 259.551 MiB | 是 | 63.328 ms | 完整 | 0 |

Working Set 从 1 到 9 终端的边际增量约为 3.702 MiB/终端，从 9 到 16 终端约为 2.502 MiB/终端；两个容量区间的中位估计为 3.102 MiB/终端，低于 8 MiB/终端预算。16 终端峰值 Working Set 为 231.289 MiB，低于 256 MiB 预算。Private 指标单独列出，不与 Working Set 预算混用。

三个容量点均在持续输出压力下低于 1% 主进程平均 CPU。该条件强于“空闲 CPU 不高于 1%”的最低门禁，但只代表本机、本构建和本轮负载，不能外推为所有硬件上的固定值。

## 重绘门控与输出完整性

- 9 终端：放大终端采样期 192 次画面变化全部立即请求重绘；后台终端 193 次画面变化全部被门控抑制。
- 16 终端：放大终端采样期 206 次画面变化全部立即请求重绘；后台终端 205 次画面变化全部被门控抑制。
- 1、9、16 三个容量点均读到每个真实 PowerShell 负载的 `AGENTWORKSPACE_DONE` 最终标记，证明缩小或隐藏只抑制 UI 重绘，没有暂停 PTY 读取或丢失尾部输出。
- 32 次循环切换前后 PowerShell PID 集合不变，说明聚焦切换没有重启终端进程。

## 视觉证据

- [1 终端矩阵总览](性能数据/capacity-01终端-20260718-184403/矩阵总览.png)
- [9 终端矩阵总览](性能数据/capacity-09终端-20260718-185048/矩阵总览.png)
- [16 终端矩阵总览](性能数据/capacity-16终端-20260718-185246/矩阵总览.png)
- [16 终端单窗口放大与文件树](性能数据/capacity-16终端-20260718-185246/单窗口放大.png)

## 原始证据

每个运行目录包含 `运行结果.json`、`进程采样.csv`、`矩阵总览.png` 和 `单窗口放大.png`。运行期间用于恢复本机应用状态的备份未纳入证据提交。

- `性能数据/capacity-01终端-20260718-184403/`
- `性能数据/capacity-09终端-20260718-185048/`
- `性能数据/capacity-16终端-20260718-185246/`

## 尚未完成

- 9 终端连续运行 30 分钟的长稳门禁。
- 长稳结束后的 Working Set 稳定区间判断、所有最终标记、正常关闭和零残留核对。
