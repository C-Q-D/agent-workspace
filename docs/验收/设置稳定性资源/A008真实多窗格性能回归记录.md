# A008 真实 9/16 窗格性能回归记录

## 结论边界

2026-07-25，当前 Release 产品代码和提交 `fc0b53d18c6edd18aafa5d633345ec5e3d58ee4f`
完成 9 与 16 个真实 PowerShell/ConPTY 的设置稳定性资源门禁。这里证明的是
AgentWorkspace 桌面宿主与空闲/输出 PowerShell 的额外开销，不代表 9 或 16 个 Codex、
Claude Code 等真实 AI CLI 的资源消耗。

## 复跑命令

```powershell
cargo build -p paneflow-app --release --locked

pwsh -NoProfile -File .\scripts\运行Windows设置稳定性资源门禁.ps1 `
  -BinaryPath .\target\release\agent-workspace.exe `
  -OutputDirectory .\docs\验收\设置稳定性资源\运行数据 `
  -KeepFixture
```

最终机器结果：

```text
docs/验收/设置稳定性资源/运行数据/Release-20260725-105622/门禁结果.json
```

二进制 SHA-256：

```text
f98c6198937a06c6f65e7fd8cf8d30fafb565675c0dfde1e492ad2c7cf79a5ad
```

## 基线机器

| 项目 | 值 |
|---|---|
| Windows | Microsoft Windows 11 专业版，10.0.26200，64 位 |
| 物理内存 | 31.636 GiB |
| CPU | 13th Gen Intel Core i7-13700H |
| 逻辑处理器 | 20 |
| PowerShell | 7.6.3 |
| 采样间隔 | 1000 ms |
| 每阶段样本 | 10 |
| 每阶段时间窗 | 约 9 秒首尾跨度，10 个绝对节拍样本 |

冷启动采样分别在进程启动后 8.600 ms 与 2.398 ms 开始，先完成十个资源样本再观察
IPC 就绪。因此结果中的约 9 秒是“就绪观察上界”，不能当作真实启动耗时；同会话重启
实际观察到就绪分别为 1678.000 ms 与 1838.657 ms。

## 宿主资源结果

CPU 使用相邻阶段首尾样本的宿主累计 CPU 差值，按实际毫秒跨度和 20 个逻辑处理器归一化。
Private Memory、线程和句柄取阶段十个样本的最大值。

| 窗格 | 阶段 | 宿主 CPU | 宿主 Private 峰值 | 线程峰值 | 句柄峰值 |
|---:|---|---:|---:|---:|---:|
| 9 | 冷启动 | 0.131% | 124.855 MiB | 21 | 499 |
| 9 | 稳定空闲 | 0.347% | 164.367 MiB | 52 | 644 |
| 9 | 活动输出 | 1.105% | 168.676 MiB | 54 | 652 |
| 9 | 聚焦文件/Git | 0.260% | 166.973 MiB | 51 | 648 |
| 16 | 冷启动 | 0.182% | 124.930 MiB | 21 | 499 |
| 16 | 稳定空闲 | 0.478% | 217.828 MiB | 72 | 735 |
| 16 | 活动输出 | 1.146% | 221.453 MiB | 81 | 753 |
| 16 | 聚焦文件/Git | 0.338% | 219.918 MiB | 74 | 745 |

同一阶段中的 PowerShell Shell 单独分组：9 窗格最高约 571.074 MiB Private，16 窗格
最高约 1025.062 MiB Private。本轮没有启动 Codex、Claude、Node、Bun 或 Deno，CLI
分组进程峰值为 0；不能把 Shell 或未来真实 CLI 的资源并入宿主数字。

## 用户行为与生命周期

- 9 和 16 个工作区、Surface、PowerShell 数量均精确一致，没有窗格丢失。
- 每个场景直接交叉写入两轮、每轮十个稳定设置键；第二轮后没有 watcher 额外回写，
  未知兄弟字段保留。
- 活动输出阶段每个真实终端都观察到 `A007-ACTIVE-DONE`，不是空白假窗口。
- 聚焦阶段使用不抢前台的目标窗口消息进入首个工作区；右侧显示
  `上下文文件.txt`，左侧 Git 状态显示 `master +1`。截图由 `PrintWindow` 只绘制
  AgentWorkspace：
  - `09终端/聚焦文件Git上下文.png`
  - `16终端/聚焦文件Git上下文.png`
- 聚焦后主动点击恢复；Surface ID 与当前 PowerShell PID 均未改变。
- 两个场景都正常关闭并使用同一用户、配置和会话重新启动；稳定 workspace ID 恢复，
  PowerShell PID 更换，证明真实 PTY 按产品契约重新创建。
- 两次运行的完整进程树都正常退出，最终 AgentWorkspace 残留进程为 0。

## 日志说明

直接从 Cargo `target/release` 运行时，每个终端记录一次“packaged Windows helper dir
missing”警告。该警告表示未使用安装包/便携包的 helper 载荷，AI hook 侧栏不会激活；
本轮只验收普通 PowerShell/ConPTY，且 CLI 分组为 0，因此不影响当前结论。A033～A038
将单独审查默认发行载荷和 helper 边界，本文不提前声明发行包 helper 已通过。

## 根因与失败证据

正式通过前保留了五个独立修复：

- `ef115cd`：补齐线程与句柄分组采样。
- `1a130f5`：每个场景增加同会话重启恢复。
- `c62ce8c`：修复 PowerShell `switch` 重绑定进程自动变量。
- `0572c6d`：空 Shell/CLI 分组的数值汇总归零。
- `fc0b53d`：把冷启动采样移到 IPC 就绪前，并写入机器基线。

对应根因文档与运行日志均保存在本目录和 `失败数据/`，没有删除失败现场。A008 只封存
真实性能回归；是否关闭 ATOM-40-04 与 P2-05 留给 A009 的总验收。
