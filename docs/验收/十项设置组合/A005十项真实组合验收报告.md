# A005 十项真实组合验收报告

## 结论

2026-07-25，AgentWorkspace Release 在隔离用户目录中完成两轮十项设置页交叉修改，并在
正常退出、重启后恢复第二轮全部值。当前窗口、工作区、Surface 和 PowerShell/ConPTY 在
两轮即时设置期间身份不变；两次应用运行均正常退出，最终残留进程数为 0。

- 结果：通过。
- 验收基线提交：`335f8410e1c895be982f12e492df70a66479cf7a`。
- 运行结果：`运行数据/Release-20260725-102553/运行结果.json`。
- Release SHA-256：见运行结果中的 `binary.sha256`。
- 隔离数据根：`F:\AWStableSettings-Release-20260725-102553\隔离用户\.agent-workspace`。
- 隔离工作区：`F:\AWStableSettings-Release-20260725-102553\真实工作区`。

## 两轮设置结果

| 磁盘键 | 初始值 | 第一轮 | 第二轮与重启后 |
|---|---|---|---|
| `theme_mode` | `system` | `dark` | `light` |
| `theme` | 应用解析默认主题 | `One Dark` | `AgentWorkspace Light` |
| `font_family` | 默认字体 | `Consolas` | `Cascadia Mono` |
| `font_size` | `13` | `14` | `15` |
| `default_shell` | 系统默认 | `powershell.exe` | `pwsh.exe` |
| `default_reference_format` | `common` | `claude` | `codex` |
| `workspace_grid_density` | `auto` | `compact` | `comfortable` |
| `git_auto_init` | `true` | `false` | `true` |
| `claude_code_command` | 默认 `claude` | `claude --model sonnet` | `claude --model opus` |
| `codex_command` | 默认 `codex` | `codex --sandbox workspace-write` | `codex --sandbox read-only` |

两轮均由真实设置页控件写入；验收脚本只建立初始配置并观察两轮目标，没有直接写入结果。
未知字段 `future_setting.preserved` 在十项窄写入和重启后仍为 `true`。

## 生命周期证据

首次运行两轮修改期间：

- 窗口句柄保持不变。
- 应用 PID 保持不变。
- 稳定 `workspace_id` 保持为 `1`。
- Surface ID 保持为 `12884901889`。
- PowerShell PID 保持为 `14048`。

这证明主题、字体和矩阵密度的即时更新没有重建当前窗口或 PTY；Shell、启动命令、引用
格式与 Git 策略只改变对应的后续生命周期默认值。重启后工作区与 PowerShell 由会话恢复
重新创建，第二轮十项设置全部从磁盘恢复。

## 应用自身截图

截图均使用 `PrintWindow` 对 AgentWorkspace 窗口离屏绘制，没有截取桌面或其他软件：

- `运行数据/Release-20260725-102553/04-General-重启恢复.png`
- `运行数据/Release-20260725-102553/05-Themes-重启恢复.png`
- `运行数据/Release-20260725-102553/06-Terminal-重启恢复.png`
- `运行数据/Release-20260725-102553/07-AIAgent-重启恢复.png`

## 失败、根因与修复原子

真实验收没有删除失败证据：

1. A005-F01：`workspace.list` 未公开稳定工作区 ID，验收器错误读取不存在的 `id`。
   根因记录为 `A005-F01根因记录.md`，修复提交 `6891821`，原始失败数据位于
   `失败数据/A005-F01-Release-20260725-101020/`。
2. A005-F02：验收目标错误组合了主题模式与预设主题，形成设置页不可达状态。
   根因记录为 `A005-F02根因记录.md`，修复提交 `335f841`，原始失败数据位于
   `失败数据/A005-F02-Release-20260725-102029/`。

AI Agent 输入自动化首次尝试使用目标消息模拟 `Ctrl+A`，但系统级修饰键状态不会随目标
消息改变，控件将 `A` 当作普通字符。该偏离发生在提交后的真实磁盘值中，随后改用逐字符
Backspace 清空同一真实输入控件，再重新输入并提交；未修改生产代码，也未手工修 JSON。

## 回归结果

以下命令在最终证据生成后通过：

- `cargo test -p paneflow-config --quiet`：113 passed。
- `cargo test -p paneflow-app --quiet`：1461 passed。
- `cargo test -p paneflow-app --test flex_nchild --quiet`：5 passed。
- `cargo fmt --all -- --check`：通过。
- `pwsh -NoProfile -File scripts/测试Windows十项设置组合验收脚本.ps1 ...`：通过。

本报告只关闭 A005 的真实设置组合与生命周期验收，不提前宣称 A006 的 UNIT-40 证据封存
或 A007—A009 的多窗格性能总验收已经完成。
